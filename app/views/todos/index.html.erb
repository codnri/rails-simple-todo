<h1>index page</h1>
<div><%= link_to 'New Todo', new_todo_path %></div>

<% unless @todos.empty? %>
  <div>
    <% @todos.each do |t|%>
      <div>
        <%= t.subject %>
      </div>
    <% end %>
  </div>
<% else %>
    <p>No Todo</p>
<% end %>