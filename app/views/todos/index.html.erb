<h1>index page</h1>
<div><%= link_to 'New Todo', new_todo_path %></div>
<br/>
<% unless @todos.empty? %>
  <div>
    <% @todos.each do |t|%>
      <div>
        <%= link_to t.subject, todo_path(t) %>
      </div>
    <% end %>
  </div>
<% else %>
    <p>No Todo</p>
<% end %>